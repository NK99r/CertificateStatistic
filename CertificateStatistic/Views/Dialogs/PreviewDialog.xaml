<UserControl x:Class="CertificateStatisticWPF.Views.Dialogs.PreviewDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CertificateStatisticWPF.Views.Dialogs"
             mc:Ignorable="d" 
             Height="300" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="{Binding ImagePath}"/>
        <StackPanel Grid.Row="1" Margin="10,10">
            <TextBlock Text="{Binding Tip}" FontSize="15" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
