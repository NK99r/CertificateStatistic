<UserControl x:Class="CertificateStatisticWPF.Views.Dialogs.LoginDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CertificateStatisticWPF.Views.Dialogs"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:pwdEx="clr-namespace:DailyApp.WPF.Extensions"
             Width="600">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="NoResize"/>
        </Style>
    </prism:Dialog.WindowStyle>

    <Grid Background="AliceBlue" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5" HorizontalAlignment="Center">
            <Image Source="/Asset/pic/logo.png" Height="150" Width="150"/>
            <TextBlock Style="{StaticResource TitleTextStyle}" Foreground="SkyBlue"/>
        </StackPanel>

        <md:Transitioner Grid.Row="1" SelectedIndex="{Binding SelectedIndex, FallbackValue=0}">
            <!--密码登录视图-->
            <md:TransitionerSlide>
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
                    <!--一行手机号-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25" HorizontalAlignment="Right">
                        <TextBlock VerticalAlignment="Center" Text="手机号："/>
                        <TextBox md:HintAssist.Hint="请输入手机号" Text="{Binding AccountDTO.PhoneNum}" Width="200"/>
                    </StackPanel>

                    <!--一行密码-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25" HorizontalAlignment="Right">
                        <TextBlock VerticalAlignment="Center" Text="密码："/>
                        <PasswordBox md:HintAssist.Hint="请输入密码" pwdEx:PasswordBoxExtend.Pwd="{Binding Pwd, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200">
                            <i:Interaction.Behaviors>
                                <pwdEx:PasswordBoxBehavior></pwdEx:PasswordBoxBehavior>
                            </i:Interaction.Behaviors>
                        </PasswordBox>
                    </StackPanel>
                    
                    <DockPanel Margin="5" Height="15">
                        <TextBlock Margin="5,0" Text="注册账号" HorizontalAlignment="Left">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding ShowRegisterContentCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                        <TextBlock Margin="5,0" Text="验证码登录" HorizontalAlignment="Right">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding ShowLogin_CpaContentCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>
                    </DockPanel>
                    
                    <Button Content="登录" Command="{Binding LoginCommand}" Style="{StaticResource LoginButton}"/>
                </StackPanel>
            </md:TransitionerSlide>
            
            <!--验证码登录视图-->
            <md:TransitionerSlide>
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
                    <!--一行手机号-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25">
                        <TextBlock VerticalAlignment="Center" Text="手机号："/>
                        <TextBox md:HintAssist.Hint="请输入手机号" Text="{Binding AccountDTO.PhoneNum}" Width="200"/>
                    </StackPanel>
                    <!--一行验证码-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25">
                        <TextBlock VerticalAlignment="Center" Text="验证码："/>
                        <TextBox md:HintAssist.Hint="请输入验证码" Width="170"/>
                        <Button Margin="5,0" Height="25" Width="25" ToolTip="发送验证码" Background="Transparent" Cursor="Hand">
                            <Image Source="/Asset/pic/send.png" Width="25" Height="25"/>
                        </Button>
                    </StackPanel>

                    <DockPanel Margin="5" Height="15">
                    <TextBlock Margin="5,0" Text="注册账号" HorizontalAlignment="Left">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding ShowRegisterContentCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                    </TextBlock>
                    <TextBlock Margin="5,0" Text="密码登录" HorizontalAlignment="Right">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding ShowLogin_PwdContentCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                    </TextBlock>
                    </DockPanel>
                    <Button Content="登录" Style="{StaticResource LoginButton}"/>
                </StackPanel>
            </md:TransitionerSlide>

            <!--注册视图-->
            <md:TransitionerSlide>
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
                    <!--一行手机号-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25" HorizontalAlignment="Right">
                        <TextBlock VerticalAlignment="Center" Text="手机号："/>
                        <TextBox md:HintAssist.Hint="请输入手机号" Text="{Binding AccountDTO.PhoneNum}" Width="200"/>
                    </StackPanel>

                    <!--一行密码-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25" HorizontalAlignment="Right">
                        <TextBlock VerticalAlignment="Center" Text="密码："/>
                        <PasswordBox md:HintAssist.Hint="请输入密码" pwdEx:PasswordBoxExtend.Pwd="{Binding Pwd,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="200">
                            <i:Interaction.Behaviors>
                                <pwdEx:PasswordBoxBehavior></pwdEx:PasswordBoxBehavior>
                            </i:Interaction.Behaviors>
                        </PasswordBox>
                    </StackPanel>

                    <!--一行确认密码-->
                    <StackPanel Margin="5" Orientation="Horizontal" Height="25" HorizontalAlignment="Right">
                        <TextBlock VerticalAlignment="Center" Text="确认密码："/>
                        <PasswordBox md:HintAssist.Hint="请再次输入密码" pwdEx:PasswordBoxExtend.Pwd="{Binding ConfirmPwd,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="200">
                            <i:Interaction.Behaviors>
                                <pwdEx:PasswordBoxBehavior></pwdEx:PasswordBoxBehavior>
                            </i:Interaction.Behaviors>
                        </PasswordBox>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                        <Button Content="注册" Command="{Binding RegisterCommand}" Width="100" Style="{StaticResource LoginButton}"/>
                        <Button Content="返回登陆" Command="{Binding ShowLogin_PwdContentCommand}" Width="100" Style="{StaticResource LoginButton}"/>
                    </StackPanel>
                </StackPanel>
            </md:TransitionerSlide>
        </md:Transitioner>

        <!--显示错误提示-->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center">
            <md:Snackbar x:Name="ErrorMsgBar" Width="600" MessageQueue="{md:MessageQueue}"/>
        </StackPanel>
    </Grid>
</UserControl>
